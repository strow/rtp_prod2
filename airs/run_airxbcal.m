% Recursive directory code
% addpath /asl/matliab/fileexchange/enhanced_dir/
% rtpread, rtpwrite, etc.
addpath /asl/matlib/h4tools
% pattr function
addpath /asl/matlib/rtptools

% % rdir syntax let's you use '**' for any dir name
% fnlist = rdir(fullfile(dn,'2013','**','*.hdf'))
% 
% % Pick a random file to read
% n = length(fnlist);
% i = round(n/2);
% 
